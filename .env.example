# Supabase (client-safe public keys only)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# NOTE: Service-role keys, payment provider secrets, maps/SMS/WhatsApp and AI
# provider credentials must live ONLY in Supabase Edge Function secrets — never
# in the frontend or any VITE_* variable.
